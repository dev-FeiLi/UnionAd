package io.union.log.dao;

import io.union.log.entity.LogEffect;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogEffectRepository extends JpaRepository<LogEffect, Long> {
}
